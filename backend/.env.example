DATABASE_URL=postgres://rafay@localhost:5432/barber_dev
SESSION_SECRET=change-me-to-a-long-random-string
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
# Where style card photos are stored. Defaults to <repo>/uploads (gitignored).
UPLOADS_DIR=

# Only needed once shop_settings.messaging_mode is switched to 'live' — the
# default 'dry_run' mode never touches these. Never commit real values.
WHATSAPP_API_URL=
WHATSAPP_TOKEN=

# Seed script only — never commit real values.
OWNER_USERNAME=owner
OWNER_PASSWORD=
OWNER_NAME=Owner
SHOP_NAME=The Shop
